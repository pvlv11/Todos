<Window x:Class="Todos.ApiKeyDialogWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Todos"
        mc:Ignorable="d"
        Title="Provide connection apikey" Height="100" Width="250">
    <Grid Margin="5">

        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <StackPanel Grid.Row="0" Grid.Column="0" >
            <PasswordBox x:Name="passwordBoxApikey" Width="120" Margin="0,5,0,0"/>
            <Button x:Name="btnSaveApikey" Margin="0,5,0,0" Content="Save apikey" Width="80" Click="btnSaveApikey_Click"/>
        </StackPanel>




    </Grid>
</Window>
